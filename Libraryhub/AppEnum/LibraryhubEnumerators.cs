namespace Libraryhub.AppEnum
{
    public enum BookActivityStatus
    {
        Check_Out = 1,
        Check_In,
        Sold
    }
    public enum Switch
    {
        OFF = 0,
        ON = 1,
    }
    public enum OrderStatus
    {
        Not_Delivered = 0,
        Delivered = 1  
    }

    public enum Role
    {
        Admin = 1,
        User
    }
}
